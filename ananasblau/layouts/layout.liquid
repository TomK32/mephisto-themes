<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
  <title>{% if article %}{{ article.title }}{% endif %} -- {{ site.title }}</title>
  <meta name="description" content="This is the {{site.title}} blog" />
  <meta name="keywords" content="blog,{{site.title}}" />
  <meta name="generator" content="Mephisto" />
  {{ 'main' | stylesheet }}
  {{ site.home_section | articles_feed }}
</head>
<body>

  <div id="wrap">
    <div id="header"><h1><a href="/">{{ site.title }}</a></h1>
      <p class="description">{{ site.subtitle }}</p>
    </div>
    <div id="content">
      {{ content_for_layout }}
    </div>
    <div id="sidebar">
      <h2>Search</h2>
      <form method="get" id="sform" action="/search">
        <input type="text" id="q" value="" name="q" size="15" />
        <input type="submit" value="go" />
      </form>
        
      {% if site.page_sections %}
      <h2>Pages</h2>
      <ul>
{% for page in site.page_sections %}
        <li>{{ page | link_to_section }}</li>{% endfor %}
      </ul>
      {% endif %}

      <h2>Categories</h2>
      <ul>
        {% for category in site.blog_sections %}
        <li>{{ category | link_to_section  }}</li>{% endfor %}
      </ul>
    </div>
  </div>

  <div id="footer">{{ site.title }} is using the <a href="http://ananasblau.com/themes/ananasblau">ananasblau theme</a> and running <a href="http://mephistoblog.com/">Mephisto</a></div>
</body>
</html>
